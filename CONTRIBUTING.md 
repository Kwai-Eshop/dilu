## 项目说明：

采用yarn + lerna 的多包管理，yarn负责依赖管理，lerna负责发包，采用固定版本的模式；

### 目录结构说明
```
├── example          // 主子应用demo，包括React版本的主子应用，Vue版本的主子应用；
└── packages         // vue、react版本的SDK源码
    └── core         // 的卢SDK的核心包，提供公共方法和类型定义等
    └── react        // react版本的主应用SDK
    └── react-micro  // react版本的子应用SDK
    └── vue2         // vue2版本的主应用SDK
    └── vue2-micro   // vue2版本的子应用SDK
    └── vue3         // vue3版本的主应用SDK
    └── vue3-micro   // vue3版本的子应用SDK
```
